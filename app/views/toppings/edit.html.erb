<h1>Edit topping</h1>
 
<%= form_with(model: @topping) do |form| %>
 
  <% if @topping.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@topping.errors.count, "error") %> prohibited
        this topping from being saved:
      </h2>
      <ul>
        <% @topping.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name%>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', toppings_path %>